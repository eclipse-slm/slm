---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  no_log: "{{ molecule_no_log }}"
  tasks:
    - name: Destroy via kubevirt
      ansible.builtin.include_role:
        name: fabos.molecule_kubevirt
        tasks_from: destroy.yml
