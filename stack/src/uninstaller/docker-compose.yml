services:

  uninstaller:
    build: .
    image: ghcr.io/eclipse-slm/slm/uninstaller:1.4.0-RELEASE
    restart: no
    volumes:
      - "//var/run/docker.sock:/var/run/docker.sock"
      - "./src:/src"
