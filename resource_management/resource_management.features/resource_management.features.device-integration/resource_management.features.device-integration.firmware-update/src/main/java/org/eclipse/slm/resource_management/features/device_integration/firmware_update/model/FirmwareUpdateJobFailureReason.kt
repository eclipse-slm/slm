package org.eclipse.slm.resource_management.features.device_integration.firmware_update.model

enum class FirmwareUpdateJobFailureReason {
    UNKNOWN_ERROR,
    INVALID_CREDENTIALS,
    FIRMWARE_VERSION_ALREADY_INSTALLED,
}