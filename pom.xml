<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.eclipse.slm</groupId>
    <artifactId>slm</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <modules>
        <module>common</module>
        <module>platform_management</module>
        <module>resource_management</module>
        <module>service_management</module>
        <module>ui</module>
        <module>notification_service</module>
        <module>information_service</module>
        <module>catalog</module>
    </modules>

    <name>Eclipse Service Lifecycle Management</name>
    <description>Eclipse SLM core components.</description>
    <url>https://github.com/eclipse-slm/slm</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/apache-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>eclipse-slm</id>
            <name>Eclipse SLM Contributors</name>
            <organization>Eclipse SLM</organization>
            <organizationUrl>https://github.com/eclipse-slm</organizationUrl>
        </developer>
    </developers>

    <organization>
        <name>Eclipse SLM</name>
        <url>https://github.com/eclipse-slm</url>
    </organization>

    <scm>
        <connection>scm:git:https://github.com/eclipse-slm/slm.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/eclipse-slm/slm.git</developerConnection>
        <url>https://github.com/eclipse-slm/slm</url>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/eclipse-slm/slm/issues</url>
    </issueManagement>

    <ciManagement>
        <system>GitHub Actions</system>
        <url>https://github.com/eclipse-slm/slm/actions</url>
    </ciManagement>

    <profiles>
        <profile>
            <id>e2e-tests</id>
            <modules>
                <module>tests</module>
            </modules>
        </profile>
    </profiles>

    <properties>
        <revision>1.5.0-SNAPSHOT</revision>

        <java.version>21</java.version>
        <kotlin.version>2.2.21</kotlin.version>
        <jackson.version>2.17.2</jackson.version>
        <resource.delimiter>^</resource.delimiter>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <project.reporting.outputDirectory>${project.build}</project.reporting.outputDirectory>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <maven.plugin.surefire.version>3.5.4</maven.plugin.surefire.version>
        <maven.plugin.surefire-report.version>3.5.4</maven.plugin.surefire-report.version>
        <maven.plugin.failsafe.version>3.5.4</maven.plugin.failsafe.version>
        <maven.plugin.docker.fabric8.version>0.48.1</maven.plugin.docker.fabric8.version>
        <maven.plugin.swagger-codegen.version>3.0.24</maven.plugin.swagger-codegen.version>
        <maven.plugin.spring.boot.version>${spring.boot.version}</maven.plugin.spring.boot.version>
        <maven.plugin.apache.compiler.version>3.13.0</maven.plugin.apache.compiler.version>
        <maven.plugin.apache.deploy.version>3.1.3</maven.plugin.apache.deploy.version>
        <maven.plugin.apache.resources.version>3.2.0</maven.plugin.apache.resources.version>
        <maven.plugin.codehaus.exec.version>3.5.0</maven.plugin.codehaus.exec.version>
        <maven.plugin.jacoco.version>0.8.14</maven.plugin.jacoco.version>
        <maven.plugin.coveralls.version>5.0.0</maven.plugin.coveralls.version>
        <maven.plugin.openapi-generator.version>7.20.0</maven.plugin.openapi-generator.version>
        <maven.plugin.deploy.version>3.1.3</maven.plugin.deploy.version>
        <maven.plugin.dokka>2.2.0</maven.plugin.dokka>
        <maven.plugin.source>3.3.1</maven.plugin.source>
        <maven.plugin.central-publishing>0.10.0</maven.plugin.central-publishing>
        <maven.plugin.gpg>3.2.8</maven.plugin.gpg>
        <gpg.skip>true</gpg.skip>

        <common.awx.client.version>1.0.0-SNAPSHOT</common.awx.client.version>

        <spring.boot.version>3.3.9</spring.boot.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <spring-cloud-contract-wiremock.version>4.1.4</spring-cloud-contract-wiremock.version>
        <spring.statemachine.version>4.0.0</spring.statemachine.version>
        <springaddons-c4-soft.version>7.8.10</springaddons-c4-soft.version>
        <springdoc.openapi.version>2.5.0</springdoc.openapi.version>
        <swagger.version>2.2.22</swagger.version>
        <keycloak.version>26.0.8</keycloak.version>
        <migbase.version>2.2</migbase.version>
        <joda-time.version>2.12.7</joda-time.version>
        <mariadb.client.version>3.4.1</mariadb.client.version>
        <model.mapper.version>3.2.6</model.mapper.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <github.docker.java.version>3.2.13</github.docker.java.version>
        <apache.commons.version>3.12.0</apache.commons.version>
        <apache.httpcomponents.version>4.5.14</apache.httpcomponents.version>
        <apache.httpcomponents-client5.version>5.6</apache.httpcomponents-client5.version>
        <apache.httpcomponents-client5.core.version>5.4.1</apache.httpcomponents-client5.core.version>
        <netty.version>4.1.73.Final</netty.version>
        <reactor.netty.version>1.0.15</reactor.netty.version>
        <jgit.version>7.3.0.202506031305-r</jgit.version>
        <basyx.version>2.0.0-milestone-07</basyx.version>
        <aas.aas4j.version>1.0.2</aas.aas4j.version>
        <aas.sdk.version>1.0.0-SNAPSHOT</aas.sdk.version>
        <minio.version>8.5.7</minio.version>
        <commons.io.version>2.18.0</commons.io.version>
        <hypersistence-utils-version>3.8.2</hypersistence-utils-version>
        <uuid.generator.version>5.0.0</uuid.generator.version>
        <resteasy.version>6.2.12.Final</resteasy.version>
        <fastexcel.version>0.19.0</fastexcel.version>
        <jakarta.ws.version>4.0.0</jakarta.ws.version>
        <feign.version>13.8</feign.version>

        <grpc.version>1.66.0</grpc.version>
        <reactor-grpc.version>1.2.4</reactor-grpc.version>
        <protobuf-java.version>3.25.4</protobuf-java.version>
        <protobuf-maven-plugin.version>2.5.0</protobuf-maven-plugin.version>

        <testcontainers.version>1.20.1</testcontainers.version>
        <testcontainers.keycloak.version>4.0.1</testcontainers.keycloak.version>
        <assertj.version>3.26.3</assertj.version>
        
        <docker.image.registry>ghcr.io/eclipse-slm</docker.image.registry>
        <docker.image.skip>false</docker.image.skip>

        <jacoco.version>0.8.14</jacoco.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Common Modules !-->
            <dependency>
                <groupId>org.eclipse.slm</groupId>
                <artifactId>awx.client</artifactId>
                <version>${common.awx.client.version}</version>
                <scope>compile</scope>
            </dependency>
            <dependency>
                <groupId>org.eclipse.slm</groupId>
                <artifactId>awx.model</artifactId>
                <version>${common.awx.client.version}</version>
                <scope>compile</scope>
            </dependency>
            <!-- Kotlin !-->
            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-bom</artifactId>
                <version>${kotlin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Jackson !-->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.module</groupId>
                <artifactId>jackson-module-kotlin</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.module</groupId>
                <artifactId>jackson-module-jakarta-xmlbind-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <!-- OpenAPI !-->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.openapi.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations</artifactId>
                <version>${swagger.version}</version>
            </dependency>
            <!-- MariaDB !-->
            <dependency>
                <groupId>org.mariadb.jdbc</groupId>
                <artifactId>mariadb-java-client</artifactId>
                <version>${mariadb.client.version}</version>
            </dependency>
            <!-- Testing !-->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit-jupiter.version}</version>
                <scope>test</scope>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>org.jacoco</groupId>
                <artifactId>org.jacoco.agent</artifactId>
                <version>${jacoco.version}</version>
                <classifier>runtime</classifier>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>mariadb</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- Utils !-->
            <dependency>    <!-- Base64 encoding !-->
                <groupId>com.brsanthu</groupId>
                <artifactId>migbase64</artifactId>
                <version>${migbase.version}</version>
            </dependency>
            <dependency>    <!-- Model mapping !-->
                <groupId>org.modelmapper</groupId>
                <artifactId>modelmapper</artifactId>
                <version>${model.mapper.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>
            <dependency>    <!-- Model Mapping !-->
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <!-- HTTP !-->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${apache.commons.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.httpcomponents</groupId>
                <artifactId>httpclient</artifactId>
                <version>${apache.httpcomponents.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.httpcomponents.client5</groupId>
                <artifactId>httpclient5</artifactId>
                <version>${apache.httpcomponents-client5.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.httpcomponents.core5</groupId>
                <artifactId>httpcore5</artifactId>
                <version>${apache.httpcomponents-client5.core.version}</version>
            </dependency>
            <!-- AAS !-->
            <dependency>
                <groupId>org.eclipse.digitaltwin.aas4j</groupId>
                <artifactId>aas4j-model</artifactId>
                <version>${aas.aas4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.eclipse.digitaltwin.aas4j</groupId>
                <artifactId>aas4j-dataformat-aasx</artifactId>
                <version>${aas.aas4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.esotericsoftware.yamlbeans</groupId>
            <artifactId>yamlbeans</artifactId>
            <version>1.15</version>
        </dependency>
        <!-- UUID !-->
        <dependency>
            <groupId>com.fasterxml.uuid</groupId>
            <artifactId>java-uuid-generator</artifactId>
            <version>${uuid.generator.version}</version>
        </dependency>
        <!-- Serialization !-->
        <dependency>
            <groupId>com.fasterxml.jackson.module</groupId>
            <artifactId>jackson-module-kotlin</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <!-- Testing !-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-commons</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-inline</artifactId>
            <version>5.2.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest-all</artifactId>
            <version>1.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Kotlin !-->
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib</artifactId>
            <version>${kotlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib-jdk8</artifactId>
            <version>${kotlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-test</artifactId>
            <version>${kotlin.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
        </repository>
        <repository>
            <id>central-snapshots</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <snapshots><enabled>true</enabled></snapshots>
        </repository>
    </repositories>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <excludes>
                    <exclude>truststore/*</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src/main/resources/truststore</directory>
                <filtering>false</filtering>
            </resource>
        </resources>

        <pluginManagement>
            <plugins>
                <!-- Spring Boot !-->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${maven.plugin.spring.boot.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                                <goal>build-info</goal>
                            </goals>
                            <configuration>
                                <classifier>exec</classifier>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <!-- Deploy !-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven.plugin.apache.deploy.version}</version>
                </plugin>
                <!-- Docker Image !-->
                <plugin>
                    <groupId>io.fabric8</groupId>
                    <artifactId>docker-maven-plugin</artifactId>
                    <version>${maven.plugin.docker.fabric8.version}</version>
                    <executions>
                        <execution>
                            <id>build-image</id>
                            <phase>package</phase>
                            <goals>
                                <goal>build</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>push-image</id>
                            <phase>deploy</phase>
                            <goals>
                                <goal>push</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <skip>${docker.image.skip}</skip>
                        <images>
                            <image>
                                <name>${docker.image.registry}/${docker.image.name}</name>
                                <build>
                                    <dockerFile>${docker.dockerfile.path}</dockerFile>
                                    <contextDir>${project.basedir}</contextDir>
                                    <tags>
                                        <tag>${project.version}</tag>
                                    </tags>
                                </build>
                            </image>
                        </images>
                    </configuration>
                </plugin>
                <!-- Resources !-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven.plugin.apache.resources.version}</version>
                    <configuration>
                        <delimiters>
                            <delimiter>${resource.delimiter}</delimiter>
                        </delimiters>
                        <useDefaultDelimiters>false</useDefaultDelimiters>
                    </configuration>
                    <executions>
                        <execution>
                            <id>copy-common-sh</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>copy-resources</goal>
                            </goals>
                            <configuration>
                                <outputDirectory>${project.basedir}/target/copied-sources</outputDirectory>
                                <resources>
                                    <resource>
                                        <directory>${project.basedir}/../../../common/common.parent/common.parent.service/src/docker</directory>
                                        <includes>
                                            <include>common.sh</include>
                                            <include>startup.sh</include>
                                            <include>Dockerfile</include>
                                        </includes>
                                    </resource>
                                </resources>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <!-- Code Coverage | Jacoco !-->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${maven.plugin.jacoco.version}</version>
                </plugin>
                <!-- Publishing !-->
                <plugin>
                    <groupId>org.sonatype.central</groupId>
                    <artifactId>central-publishing-maven-plugin</artifactId>
                    <version>${maven.plugin.central-publishing}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <deploymentName>${maven.central.deployment.name}</deploymentName>
                        <publishingServerId>central</publishingServerId>
                        <autoPublish>true</autoPublish>
                        <waitUntil>published</waitUntil>
                        <centralSnapshotsUrl>https://central.sonatype.com/repository/maven-snapshots/</centralSnapshotsUrl>
                    </configuration>
                </plugin>
                <!-- GPG Signing !-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven.plugin.gpg}</version>
                    <configuration>
                        <skip>${gpg.skip}</skip>
                    </configuration>
                    <executions>
                        <execution>
                            <id>sign-artifacts</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>sign</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!-- Deployment !-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <!-- Testing !-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.plugin.surefire.version}</version>
                <configuration>
                    <argLine>${surefireArgLine}</argLine>
                    <systemPropertyVariables>
                        <api.version>1.44</api.version> <!-- Fix for https://github.com/testcontainers/testcontainers-java/issues/11212 !-->
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${maven.plugin.failsafe.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <api.version>1.44</api.version> <!-- Fix for https://github.com/testcontainers/testcontainers-java/issues/11212 !-->
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                        <configuration>
                            <argLine>${failsafeArgLine}</argLine>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Reports | Maven Surefire !-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-report-plugin</artifactId>
                <version>${maven.plugin.surefire-report.version}</version>
                <configuration>
                    <aggregate>true</aggregate>
                    <reportsDirectories>
                        <reportsDirectory>${project.build.directory}/test-reports</reportsDirectory>
                    </reportsDirectories>
                </configuration>
                <executions>
                    <execution>
                        <id>surefire-report</id>
                        <phase>site</phase>
                        <goals>
                            <goal>report-only</goal>
                            <goal>failsafe-report-only</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
                <version>3.12.1</version>
            </plugin>
            <!-- Code Coverage | Generate using Jacoco -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <propertyName>surefireArgLine</propertyName>
                            <excludes>
                                <exclude>**/generated-sources/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>prepare-agent-integration</id>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <propertyName>failsafeArgLine</propertyName>
                            <excludes>
                                <exclude>**/generated-sources/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Code Coverage | Jacoco Report Aggregation !-->
            <plugin>
                <groupId>io.github.olamy.maven.plugins</groupId>
                <artifactId>jacoco-aggregator-maven-plugin</artifactId>
                <version>1.0.4</version>
                <configuration>
                    <excludes>
                        <exclude>siemens/**</exclude>       <!-- Exclude code generated from protobuf files in common.driver from coverage report -->
                        <exclude>factory_x/**</exclude>     <!-- Exclude code generated from protobuf files in common.driver  from coverage report -->
                        <exclude>**/*MapperImpl*</exclude>  <!-- Exclude generated mapper implementations of MapStruct !-->
                        <exclude>org/eclipse/slm/service_management/service/client/**</exclude> <!-- Exclude auto-generated client code -->
                        <exclude>org/eclipse/slm/service_management/model/**</exclude>          <!-- Exclude auto-generated client code -->
                    </excludes>
                </configuration>
            </plugin>
            <!-- Code Coverage | Push to coveralls -->
            <plugin>
                <groupId>com.github.hazendaz.maven</groupId>
                <artifactId>coveralls-maven-plugin</artifactId>
                <version>${maven.plugin.coveralls.version}</version>
                <configuration>
                    <jacocoReports>
                        <jacocoReport>${project.build.directory}/site/jacoco-aggregate/jacoco.xml</jacocoReport>
                    </jacocoReports>
                    <scanForSources>true</scanForSources>
                    <sourceDirectories>
                        <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
                        <sourceDirectory>${project.basedir}/src/main/java</sourceDirectory>
                        <sourceDirectory>${project.basedir}/src/target/generated-sources</sourceDirectory>
                        <sourceDirectory>**/kotlin/*</sourceDirectory>
                    </sourceDirectories>
                </configuration>
            </plugin>
            <!-- Compilation | Kotlin !-->
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <executions>
                    <execution>
                        <id>compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <sourceDirs>
                                <sourceDir>${project.basedir}/src/main/kotlin</sourceDir>
                                <sourceDir>${project.basedir}/src/main/java</sourceDir>
                            </sourceDirs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>test-compile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>test-compile</goal>
                        </goals>
                        <configuration>
                            <sourceDirs>
                                <sourceDir>${project.basedir}/src/test/kotlin</sourceDir>
                                <sourceDir>${project.basedir}/src/test/java</sourceDir>
                            </sourceDirs>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <jvmTarget>${java.version}</jvmTarget>
                </configuration>
            </plugin>
            <!-- Compilation | Java !-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.plugin.apache.compiler.version}</version>
                <configuration>
                    <release>${java.version}</release>
                    <compilerArgument>-Xlint:unchecked</compilerArgument>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
                <executions>
                    <!-- Replacing default-compile as it is treated specially by maven -->
                    <execution>
                        <id>default-compile</id>
                        <phase>none</phase>
                    </execution>
                    <!-- Replacing default-testCompile as it is treated specially by maven -->
                    <execution>
                        <id>default-testCompile</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>java-compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>java-test-compile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Versioning !-->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.2.7</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Sources !-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven.plugin.source}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Documentation !-->
            <plugin>
                <groupId>org.jetbrains.dokka</groupId>
                <artifactId>dokka-maven-plugin</artifactId>
                <version>${maven.plugin.dokka}</version>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <phase>package</phase>
                        <goals>
                            <goal>javadocJar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
